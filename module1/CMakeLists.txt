# gcc -I
# INCLUDE_DIRECTORIES(module1)
 
# ADD_SUBDIRECTORY(module1)

# SET(SRC_LIST module1.c)
AUX_SOURCE_DIRECTORY(. SRC_LIST)
 
ADD_LIBRARY(module1 ${SRC_LIST})
 
# TARGET_LINK_LIBRARIES(module1)
# INSTALL(TARGETS DESTINATION bin)
